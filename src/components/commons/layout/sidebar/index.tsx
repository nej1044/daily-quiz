import styled from "@emotion/styled";

const SideBarDiv = styled.div`
  width: 500px;
  height: 700px;
  background-color: whitesmoke;
`;

const SideBar = () => {
  return <SideBarDiv>여기는 사이드바영역입니다.</SideBarDiv>;
};

export default SideBar;
